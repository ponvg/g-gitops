<server description="Default server">

        <!-- Enable features -->
        <featureManager>
                <feature>validator-1.0</feature>
                <feature>adminCenter-1.0</feature>
                <feature>jdbc-4.1</feature>
                <feature>jndi-1.0</feature>
                <feature>jsp-2.3</feature>
                <feature>servlet-3.1</feature>
                <feature>ssl-1.0</feature>
        </featureManager>

       <!-- Define an Administrator and non-Administrator -->
        <basicRegistry id="basic">
                <user name="admin" password="${env.consoleadminpassword}" />
                <user name="nonadmin" password="${env.consolenonadminpassword}" />
        </basicRegistry>

        <!-- Assign 'admin' to Administrator -->
        <administrator-role>
                <user>admin</user>
        </administrator-role>


        <!-- Automatically expand WAR files and EAR files . -->

        <httpEndpoint id="defaultHttpEndpoint" host="*" httpPort="9080"
                httpsPort="9443" />

        <dataSource id="DefaultDataSource" jndiName="{{ .Values.dbserver.datasourceName }}">
                <jdbcDriver libraryRef="dblib" />
                <connectionManager maxPoolSize="{{ .Values.appserver.config.database.maxPoolSize }}" minPoolSize="{{ .Values.appserver.config.database.minPoolSize }}" />
                {{- if eq (.Values.dbserver.dbvendor | lower) "oracle" }}
                <properties.oracle databaseName="{{ .Values.dbserver.dbname }}" serverName="{{ .Values.dbserver.serverName }}"
                        portNumber="{{ .Values.dbserver.port }}" user="{{ .Values.dbserver.user }}" password="${env.dbpassword}" />
                {{- else }}
                <properties.db2.jcc databaseName="{{ .Values.dbserver.dbname }}" serverName="{{ .Values.dbserver.serverName }}"
                        portNumber="{{ .Values.dbserver.port }}" user="{{ .Values.dbserver.user }}" password="${env.dbpassword}" />
                {{- end }}
        </dataSource>

        <library id="dblib">
                <fileset dir="/opt/ibm/jars/" includes="*.jar" />
        </library>

        <!-- Automatically expand WAR files and EAR files -->
        <applicationManager autoExpand="true" />
</server>
